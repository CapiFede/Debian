# input-remapper necesita /dev/uinput para inyectar teclas.
# En Debian 13 limpio el modulo no se carga solo -> la inyeccion se queda
# colgada en "Starting injection...". Esto lo carga en cada arranque.
uinput
